pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // 编译
                echo "Declarative Pipeline:Build"
            }
        }
        stage('Test') {

            steps {
                // 测试
                echo "Declarative Pipeline:Test"
            }
        }
        stage('Deploy') {
            steps {
                // 部署
                echo "Declarative Pipeline:Deploy"
            }


        }
    }
}
